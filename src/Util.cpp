#include "../include/BunnyManager.h"
#include "../include/Bunny.h"
#include "../include/Util.h"

/*void Util::display_screen(BunnyManager &bunnies)
{
    bunnies.bunny_attribute_header();

    //create iterator for bunny list

    //std::list<std::unique_ptr<Bunny>>::iterator it;

    /*for (it = bunnies.get_list().begin(); it != bunnies.get_list().end(); ++it)
    { 
        (*it)->display_bunny_values();
    }*/

    /*for (auto& it: bunnies.get_list())
    {
        it->display_bunny_values();
    }

}*/


